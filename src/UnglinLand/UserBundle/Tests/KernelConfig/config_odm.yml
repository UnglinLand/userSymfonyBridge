parameters:
    kernel.secret: odm_config_test_secret

unglin_land_user:
    driver: odm

doctrine_mongodb:
    connections:
        default:
            server: mongodb
            options: {}
    default_database: test
    document_managers:
        default:
            auto_mapping: false
